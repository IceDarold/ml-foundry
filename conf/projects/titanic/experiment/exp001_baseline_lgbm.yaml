# Baseline experiment with LightGBM using default parameters
# Uses v1_baseline feature engineering and v1_all features

defaults:
  - ../feature_engineering: v1_baseline
  - ../features: v1_all
  - _self_

# W&B tags for experiment tracking
wandb:
  tags: ["baseline", "lgbm", "exp001"]